const{spawnSync}=require('child_process'),fs=require('fs'),path=require('path');for(const line of fs.readFileSync(path.join(__dirname,'..','.env.local'),'utf8').split(/\r?\n/)){const i=line.indexOf('=');if(i>0&&!line.startsWith('#'))process.env[line.slice(0,i)]=line.slice(i+1)}const dir=path.join(__dirname,'..','backups');fs.mkdirSync(dir,{recursive:true});const stamp=new Date().toISOString().replace(/[:.]/g,'-'),file=path.join(dir,`navalha-${stamp}.dump`),pg='C:\\Program Files\\PostgreSQL\\18\\bin\\pg_dump.exe',r=spawnSync(pg,['--format=custom','--file',file,process.env.DATABASE_URL],{stdio:'inherit'});if(r.status!==0)process.exit(r.status||1);console.log('Backup criado:',file);
